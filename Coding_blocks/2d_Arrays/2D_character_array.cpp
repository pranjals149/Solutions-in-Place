#include <iostream>

using namespace std;

int main()
{
    // char a[100][100] = {{'a'}, {'b'}, {'c'}};
    // char a[100][100] = {"ab", "cd"};
    // The above two methods are used to define a 2D character array.
    cout << a[0] << endl;
    cout << a[1] << endl;
}